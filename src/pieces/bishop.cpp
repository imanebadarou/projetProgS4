#include "bishop.hpp"

Bishop::Bishop (const Color color): Piece(color){};

std::vector<std::array<int, 2>> Bishop::whereCanIMove() {
    std::vector<std::array<int, 2>> moves;
    
    
    return moves; 
}